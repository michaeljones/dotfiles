[user]
	name = Michael Jones
	email = m.pricejones@gmail.com
[alias]
	b = branch
	d = diff
	dc = diff --cached
	c = checkout
	hist = log HEAD^^^..
	fp = format-patch
	s = status
	commmit = commit
	rn = release-notes
	mn = merge --no-ff --no-edit
	fix-whitespace = !git diff | git apply --whitespace=fix
	cn = commit --no-verify
	char-diff = diff --word-diff --word-diff-regex=.
	letter-diff = diff --word-diff --word-diff-regex=.
[apply]
	whitespace = fix
[merge]
	tool = meld
	conflictstyle = diff3
[color]
	diff = auto
	ui = auto
[push]
	default = nothing
[gc]
	auto = 0
[rebase]
	autosquash = true
[rerere]
	enabled = true
[diff]
	algorithm = patience
	compactionHeuristic = 1
